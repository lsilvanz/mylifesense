# Copy to .env.local and fill in. Without these the app uses localStorage.
# The anon key is a public/publishable key — safe in client code (RLS protects data).
NEXT_PUBLIC_SUPABASE_URL=https://ibicedsscxhixrjekodn.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key
