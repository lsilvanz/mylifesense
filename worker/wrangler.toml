name = "mylifesense-reminders"
main = "src/index.js"
compatibility_date = "2024-11-01"

# Runs every 15 minutes; the Worker itself decides which reminders are due.
[triggers]
crons = ["*/15 * * * *"]
